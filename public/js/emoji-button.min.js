document.addEventListener('DOMContentLoaded', () => {
    const emojiButton = document.getElementById('emojiButton');
    const emojiPicker = document.getElementById('emoji-picker');
    const msgInput = document.getElementById('msgInput');

    // Vérification des éléments dans le DOM
    if (!emojiButton || !emojiPicker || !msgInput) {
        console.error("Un ou plusieurs éléments requis sont introuvables dans le DOM.");
        return;
    }

    // Initialisation des émojis
    const emojis = [
        '😀', '😂', '😍', '😢', '😡', '👍', '👎', '👏', '🙃', '😎', 
        '🥳', '😱', '💔', '❤️', '🤔', '🤗', '😇', '🤡', '😴', '😜'
    ];

    // Ajouter les émojis au conteneur du sélecteur
    emojiPicker.innerHTML = emojis.map(emoji => `<span class="emoji">${emoji}</span>`).join('');

    // Masquer ou afficher le sélecteur d'émojis
    emojiButton.addEventListener('click', () => {
        const isHidden = emojiPicker.style.display === 'none' || !emojiPicker.style.display;
        emojiPicker.style.display = isHidden ? 'flex' : 'none';
    });

    // Gestion de l'ajout d'émojis au champ de texte
    emojiPicker.addEventListener('click', (event) => {
        if (event.target.classList.contains('emoji')) {
            const selectedEmoji = event.target.textContent;
            msgInput.value += selectedEmoji; // Ajouter l'émoji sélectionné
            emojiPicker.style.display = 'none'; // Masquer le sélecteur après la sélection
        }
    });

    // Styles par défaut pour le sélecteur d'émojis
    emojiPicker.style.display = 'none'; // Initialement masqué
    emojiPicker.style.position = 'absolute';
    emojiPicker.style.border = '1px solid #ddd';
    emojiPicker.style.borderRadius = '8px';
    emojiPicker.style.padding = '10px';
    emojiPicker.style.backgroundColor = '#fff';
    emojiPicker.style.boxShadow = '0px 4px 8px rgba(0, 0, 0, 0.1)';
    emojiPicker.style.flexWrap = 'wrap';
    emojiPicker.style.justifyContent = 'center';
    emojiPicker.style.gap = '10px';
});
